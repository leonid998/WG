# Переменные окружения для 3X-UI VPN
# =====================================
# ИНСТРУКЦИЯ: Скопируйте этот файл в .env и измените значения на реальные
# Команда: cp .env.example .env

# === НАСТРОЙКИ СЕРВЕРА ===
# IP адрес вашего VPS сервера (заменить на реальный!)
SERVER_IP=YOUR_SERVER_IP

# Порт веб-панели управления (по умолчанию 54321)
# Рекомендуется изменить на нестандартный для безопасности
PANEL_PORT=54321

# === НАСТРОЙКИ VPN ===
# Порт для VPN соединений (рекомендуется 443 для обхода блокировок в РФ)
# Порт 443 маскирует VPN трафик под обычный HTTPS
VPN_PORT=443

# Протокол Reality - домен для маскировки (сайт с валидным TLS)
# Примеры: www.microsoft.com, www.cloudflare.com, www.cdn77.com
REALITY_DEST=www.microsoft.com:443

# === ПУТИ К ДАННЫМ ===
# Директория для хранения базы данных и конфигураций
DATA_PATH=./data

# Директория для SSL сертификатов
CERT_PATH=./data/cert

# === БЕЗОПАСНОСТЬ ===
# ВАЖНО: После первого запуска сменить дефолтные учетные данные!
# Логин по умолчанию: admin
# Пароль по умолчанию: admin
# Смена через веб-панель: Panel Settings → Username/Password

# === РЕЗЕРВНОЕ КОПИРОВАНИЕ ===
# Путь для бэкапов (не коммитить в Git!)
BACKUP_PATH=./backup

# Частота автобэкапов (в днях)
BACKUP_INTERVAL_DAYS=7

# === МОНИТОРИНГ ===
# Включить сбор метрик для Prometheus (опционально)
ENABLE_METRICS=false

# Порт для метрик Prometheus
METRICS_PORT=9090

# === ДОПОЛНИТЕЛЬНО ===
# Таймзона сервера (для логов)
TZ=Europe/Moscow

# Лимит одновременных подключений на пользователя (защита от перепродажи)
IP_LIMIT=1

# === ПРИМЕЧАНИЯ ===
# 1. Файл .env НЕ должен коммититься в Git (проверьте .gitignore)
# 2. Используйте сложные пароли для панели администратора
# 3. Регулярно делайте бэкапы базы данных
# 4. Для продакшена рекомендуется настроить файрвол (ufw/iptables)
