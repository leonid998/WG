# 🚀 WireGuard Quick Start для России 2025

## ⚠️ КРИТИЧНО: Обычный WireGuard блокируется!

**С июня 2024 года** российские провайдеры блокируют обычный WireGuard через DPI.

**Решение:** Использовать **AmneziaWG** (WireGuard с обфускацией)

---

## 🎯 Рекомендуемая архитектура

```
Партнеры в РФ → AmneziaWG клиент → VPS за рубежом → WhatsApp/Telegram
                (с обфускацией)        (Нидерланды/Германия)
```

---

## 📦 Быстрая установка на сервере

### Вариант 1: Автоматический скрипт (рекомендуется)

```bash
# Скачать и запустить автоустановщик
wget https://git.io/wireguard -O wireguard-install.sh
chmod +x wireguard-install.sh
sudo ./wireguard-install.sh
```

**Скрипт автоматически:**
- Установит WireGuard
- Создаст ключи
- Настроит NAT и firewall
- Создаст первого клиента с QR-кодом

### Вариант 2: AmneziaWG через Docker (для обхода блокировок)

```bash
# Установка через Docker Compose
curl -sSL https://get.amnezia.org | bash
```

---

## 🔧 Настройка для обхода блокировок в РФ

### Конфигурация сервера (AmneziaWG)

`/etc/amneziawg/awg0.conf`:

```ini
[Interface]
PrivateKey = <ВАІШ_ПРИВАТНЫЙ_КЛЮЧ>
Address = 10.0.0.1/24
ListenPort = 443  # Маскировка под HTTPS!

# Параметры обфускации для обхода DPI
Jc = 5           # Количество мусорных пакетов
Jmin = 40        # Минимальный размер
Jmax = 1200      # Максимальный размер
S1 = 0
S2 = 0
H1 = 1           # Модификация handshake
H2 = 2
H3 = 3
H4 = 4

PostUp = iptables -A FORWARD -i awg0 -j ACCEPT
PostUp = iptables -t nat -A POSTROUTING -o eth0 -j MASQUERADE
PostDown = iptables -D FORWARD -i awg0 -j ACCEPT
PostDown = iptables -t nat -D POSTROUTING -o eth0 -j MASQUERADE

[Peer]
# Клиент 1
PublicKey = <ПУБЛИЧНЫЙ_КЛЮЧ_КЛИЕНТА>
AllowedIPs = 10.0.0.2/32
```

### Конфигурация клиента (для партнеров)

```ini
[Interface]
PrivateKey = <ПРИВАТНЫЙ_КЛЮЧ_КЛИЕНТА>
Address = 10.0.0.2/32
DNS = 1.1.1.1, 1.0.0.1

# ВАЖНО: Те же параметры обфускации!
Jc = 5
Jmin = 40
Jmax = 1200
S1 = 0
S2 = 0
H1 = 1
H2 = 2
H3 = 3
H4 = 4

[Peer]
PublicKey = <ПУБЛИЧНЫЙ_КЛЮЧ_СЕРВЕРА>
Endpoint = ВАШ_IP_СЕРВЕРА:443
AllowedIPs = 0.0.0.0/0, ::/0
PersistentKeepalive = 25
```

---

## 📱 Установка клиентов для партнеров

### Windows
1. Скачать: https://download.wireguard.com/windows-client/
2. Установить AmneziaWG: https://github.com/amnezia-vpn/amneziawg-windows-client/releases
3. Импортировать конфигурацию или отсканировать QR-код

### Android
1. Google Play: "Amnezia VPN"
2. Отсканировать QR-код или импортировать `.conf` файл

### iOS
1. App Store: "Amnezia VPN"
2. Отсканировать QR-код

---

## 🔑 Создание конфигураций для партнеров

### Генерация ключей для нового клиента

```bash
# На сервере
cd /etc/wireguard
wg genkey | tee client2_private.key | wg pubkey > client2_public.key
chmod 600 client2_private.key

# Просмотр ключей
cat client2_private.key  # Приватный ключ клиента
cat client2_public.key   # Публичный ключ клиента
```

### Генерация QR-кода

```bash
# Создать конфигурацию клиента в файле client2.conf
# Затем сгенерировать QR-код
qrencode -t ansiutf8 < client2.conf
```

---

## 🛠️ Управление сервером

```bash
# Просмотр статуса
wg show

# Запуск/остановка
systemctl start wg-quick@wg0
systemctl stop wg-quick@wg0
systemctl restart wg-quick@wg0

# Автозапуск при загрузке
systemctl enable wg-quick@wg0

# Просмотр подключенных клиентов
wg show wg0 peers
```

---

## 🔐 Проверка работы VPN (для партнеров)

После подключения проверить:

```bash
# 1. Проверка IP (должен быть IP вашего сервера)
curl ifconfig.me

# 2. Проверка DNS утечек
https://dnsleaktest.com

# 3. Проверка WebRTC утечек
https://browserleaks.com/webrtc
```

---

## ⚡ Оптимизация для множества клиентов

### Увеличение производительности

```bash
# Оптимизация сетевого стека
cat >> /etc/sysctl.conf <<EOF
net.core.rmem_max = 134217728
net.core.wmem_max = 134217728
net.ipv4.tcp_rmem = 4096 87380 67108864
net.ipv4.tcp_wmem = 4096 65536 67108864
net.ipv4.tcp_congestion_control = bbr
EOF

sysctl -p
```

---

## 🚨 Troubleshooting

### VPN не подключается из России

**Проблема:** Блокировка DPI

**Решение:**
1. Убедитесь, что используете **AmneziaWG**, а не обычный WireGuard
2. Измените порт на `443` (маскировка под HTTPS)
3. Увеличьте параметры обфускации:
   ```ini
   Jc = 7
   Jmin = 50
   Jmax = 1500
   ```

### Медленная скорость

**Решение:**
1. Проверьте MTU:
   ```ini
   MTU = 1420
   ```
2. Выберите сервер ближе к России (Финляндия, Нидерланды)

### DNS утечки

**Решение:**
Добавьте Kill Switch в конфигурацию клиента:
```ini
PostUp = iptables -I OUTPUT ! -o %i -m mark ! --mark $(wg show %i fwmark) -m addrtype ! --dst-type LOCAL -j REJECT
PreDown = iptables -D OUTPUT ! -o %i -m mark ! --mark $(wg show %i fwmark) -m addrtype ! --dst-type LOCAL -j REJECT
```

---

## 📊 Сравнение решений для обхода блокировок

| Метод | Эффективность | Сложность | Скорость |
|-------|---------------|-----------|----------|
| **AmneziaWG** | ⭐⭐⭐⭐⭐ | ⭐⭐ | ⭐⭐⭐⭐⭐ |
| WG + Shadowsocks | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐⭐ |
| UDP2RAW + WG | ⭐⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐ |
| Обычный WG | ❌ Блокируется | ⭐ | ⭐⭐⭐⭐⭐ |

---

## 📚 Дополнительные ресурсы

- **Полное исследование:** `WireGuard_VPN_Russia_2025_Research.md`
- **AmneziaWG GitHub:** https://github.com/amnezia-vpn/amneziawg
- **Официальный сайт:** https://www.amnezia.org
- **Документация WireGuard:** https://www.wireguard.com

---

## ✅ Чек-лист развертывания

- [ ] Арендовать VPS за пределами РФ (Нидерланды/Германия/Финляндия)
- [ ] Установить Ubuntu 22.04 LTS
- [ ] Запустить автоустановщик WireGuard
- [ ] Настроить AmneziaWG с обфускацией
- [ ] Изменить порт на 443
- [ ] Создать конфигурации для каждого партнера
- [ ] Сгенерировать QR-коды
- [ ] Отправить партнерам QR-коды + инструкцию
- [ ] Установить клиент AmneziaWG на устройства партнеров
- [ ] Проверить подключение и отсутствие утечек DNS

---

**Дата создания:** 11 декабря 2025
**Статус:** Актуально для российских условий 2025
